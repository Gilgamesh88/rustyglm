% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rustyglm.R
\name{rustyglm}
\alias{rustyglm}
\title{Ajusta un GLM usando el motor RustyStats (Rust)}
\usage{
rustyglm(formula, data, family = "poisson", offset = NULL, weights = NULL)
}
\arguments{
\item{formula}{Fórmula R estándar, e.g. \code{ClaimNb ~ VehAge + Area}}

\item{data}{Data frame con los datos}

\item{family}{Familia de distribución: "poisson", "gaussian", "gamma",
"binomial", "tweedie", "negbinomial"}

\item{offset}{Vector numérico o nombre de columna para usar como offset.
Para frecuencia: \code{log(Exposure)}}

\item{weights}{Vector numérico de pesos prior (opcional)}
}
\value{
Objeto de clase \code{rustyglm} con coeficientes, valores ajustados
y metadatos del modelo.
}
\description{
Ajusta un GLM usando el motor RustyStats (Rust)
}
\examples{
\dontrun{
# GLM Poisson básico
mod <- rustyglm(nclaims ~ age + vehicle_type, data = datos, family = "poisson")
print(mod)
coef(mod)
}

}
